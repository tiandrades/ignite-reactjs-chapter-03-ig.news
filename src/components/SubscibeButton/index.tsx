import styles from './styles.module.scss';

export function SubscibeButton() {
  return (
    <button
      type="button"
      className={styles.subscribeButton}
    >
      Subscibe now
    </button>
  )
}